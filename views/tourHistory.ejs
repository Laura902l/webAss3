<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tour History</title>
    <!-- Bootstrap CSS -->
    <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css'>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/travel/travelagency">Travel Agency</a>
                </li>

                <li class="nav-item active">
                    <a class="nav-link" href="/travel/tourHistory">History</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/history"> Manage History</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container mt-4">
        <h1 class="text-center">Tour History</h1>
        <p class="text-center">Ticket purchase data is saved here without changes.</p>
        <table class="table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>City</th>
                    <th>Adults</th>
                    <th>Children</th>
                    <th>Total Cost</th>
                </tr>
            </thead>
            <tbody>
                <% tourHistory.forEach((tour, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= tour.cityName %></td>
                        <td><%= tour.adults %></td>
                        <td><%= tour.children %></td>
                        <td>$<%= tour.totalCost %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>

</html>
